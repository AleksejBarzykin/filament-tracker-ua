# КОТУШКА.UA

Моніторинг цін на філамент для 3D-друку в українських інтернет-магазинах
(доставка по Києву/Україні): таблиця з фільтрами по матеріалу/бренду/кольору/
магазину/ціні, історія ціни по кожній позиції, бейджі акцій та підписка на
email-сповіщення про зниження ціни.

## Стек

- Next.js (App Router) + TypeScript + Tailwind CSS 4
- Prisma + SQLite (`prisma/dev.db`, комітиться в репозиторій — див. нижче)
- Cheerio-скрапери магазинів (`scripts/scrapers`)
- Recharts для графіка історії цін

## Розробка

```bash
npm install
npm run db:push     # створити/оновити schema.prisma -> prisma/dev.db
npm run db:seed      # заповнити демо-даними (реалістичний приклад, без мережі)
npm run dev
```

## Реальний скрапінг цін

Ця сесія розробки виконувалась у пісочниці без доступу в інтернет до сайтів
магазинів, тож `prisma/dev.db` зараз заповнена **демо-даними**, а не живими
цінами. Скрапери (`scripts/scrapers/shops/*`) написані під типову розмітку
WooCommerce-магазинів за назвою товару (бренд/матеріал/колір/діаметр/вага
парсяться з тексту заголовка) — селектори варто звірити з реальним HTML при
першому запуску:

```bash
npm run scrape             # усі магазини
npm run scrape -- plexiwire  # окремий магазин (ключ з scripts/scrapers/shops/index.ts)
npm run check-notifications  # перевірити підписки на знижку і "розіслати" (зараз лише лог, TODO нижче)
```

Живий скрапінг і оновлення даних на проді керуються GitHub Actions:
`.github/workflows/scrape.yml` щодня ганяє скрапери на раннері з відкритим
інтернетом і комітить оновлений `prisma/dev.db` назад у репозиторій.

### Що доробити для продакшена

- **Селектори скраперів**: звірити з реальною версткою кожного магазину після
  першого запуску екшена (лог покаже `0 listings`, якщо селектор не підійшов).
  URL категорій підібрані через веб-пошук (без прямого доступу до самих
  сайтів із цього середовища), тож вони можуть застаріти чи не покривати
  весь асортимент.
- **OLX**: класифайд-оголошення зазвичай не мають структурованої
  schema.org/JSON-LD розмітки товару (на відміну від звичайних магазинів),
  тож `createSchemaMicrodataAdapter` для OLX, найімовірніше, поверне
  0 позицій — під нього варто написати окремий парсер конкретно під
  розмітку сторінки оголошення.
- **Rozetka/Brain**: рендеряться JS-фреймворком; адаптер розрахований на
  server-side мікророзмітку (schema.org/JSON-LD), яку такі великі майданчики
  зазвичай віддають для SEO — якщо ні, знадобиться headless-browser скрапер
  (Playwright) замість простого HTTP+cheerio.
- **Відправка email**: `scripts/check-notifications.ts` зараз лише логує, кому
  й що треба надіслати — підключити провайдера (Resend/Postmark/SMTP) в
  позначеному `TODO`.
- **Магазини/майданчики**: підключено Plexiwire, 3DPlastic,
  Filament-Shop.in.ua, UKR3D (Prom.ua), ArtLine, Brain, Rozetka, OLX.
  **Бренди**: список відомих виробників (для розпізнавання з назви товару) —
  `scripts/scrapers/utils.ts` (`KNOWN_BRANDS`), включно з Creality, Bambu Lab,
  Elegoo, Kingroon, SUNLU, eSUN та ін.
- Якщо деплоїш не на платформу з персистентним диском для SQLite — постав
  `DATABASE_URL` на зовнішню Postgres-базу і онови `datasource` у
  `prisma/schema.prisma` (`provider = "postgresql"`), або тримай `dev.db` як
  read-only снапшот, що оновлюється екшеном (поточна схема).
